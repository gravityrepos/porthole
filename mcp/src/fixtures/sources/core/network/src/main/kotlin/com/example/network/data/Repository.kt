// Fixture for GRA-201's sources.test.ts -- not part of a buildable module.
// See app/src/.../data/Repository.kt's own comment.
package com.example.network.data

class Repository {
    fun fetch(id: String): String = id
}
