POM_ARTIFACT_ID=runtime-noop
POM_NAME=Porthole Runtime (no-op)
POM_DESCRIPTION=Release-build stand-in for the Porthole runtime. Same public API, empty bodies, so instrumentation can stay in production code and compile to nothing.
